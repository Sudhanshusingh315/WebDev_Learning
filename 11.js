//Functions (Most important)
 
function onePlusAvg(a,b)
{
    let sum = 1+a+b;
    return sum;
}

console.log("The sum of the funtionc",onePlusAvg(4,5));


// New way to write a funtion (imp) use this more often, Arrow functions

const sum = ()=>
{
    return (1+r);


}

let r = prompt("Enter the value of r")
r = Number.parseInt(r);
console.log(`One is added the value of ${r} ->`, sum(r));